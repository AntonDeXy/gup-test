extends ./layout/main

block variables
  - const pageTitle = 'title111'
  - const youAreUsingPug = false

block content
  h1.title Pug - node template engine
  #container.col
    if youAreUsingPug
      p You are amazing
    else
      p(class=pageTitle) Get on it!
    p.
      Pug is a terse and simple templating language with a
      strong focus on performance and powerful features.
    p email #[a(href="" class="email") any@test.email]
block scripts
